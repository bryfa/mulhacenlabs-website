# /test — Run tests

Run the xUnit test suite and report results.

## Steps

1. Run `dotnet test` from the repo root.
2. Report the total number of tests, passed, failed, and skipped.
3. If any tests fail, show the failure details.
